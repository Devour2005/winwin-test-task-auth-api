spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:authapitest;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

data-api:
  base-url: http://localhost:9
  internal-token: test-token

jwt:
  secret: test-secret-must-be-at-least-256-bits-long-for-hs256!!
